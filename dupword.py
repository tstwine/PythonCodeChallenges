userInput = input("Does the word have any duplicate letters?")
dupWord = input



#wordList =("Please,enter")
#print ("Does the word have any duplicate letters?: %s" %dupWord)
if dupWord == 'yes' or dupWord == 'y':
	#print("Enter the duplicate words")
	returnUserInput = input()


else:
	print("Enter the duplicate words")
	returnUserInput = input()
	wordList =("Please,enter")


	
	#r.split(splits the letter in the word. then count how many times the letter appears˜)
	
# userInput = input("Please enter a word: ")
# letters = len(userInput)
# x = 0
# repeatedLetters = []

# while x < letters:
# 	if userInput.count(userInput[x]) > 1 and userInput[x] not in repeatedLetters and userInput[x] != " ":
# 		print("%s is a duplicate letter with that appears %s times in the word %s." %(userInput[x], userInput.count(userInput[x]), userInput))
# 		repeatedLetters.append(userInput[x])		
# 	x+= 1